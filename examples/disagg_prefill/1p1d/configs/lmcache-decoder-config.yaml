local_cpu: False

enable_pd: True
transfer_channel: "nixl"
pd_role: "receiver"
pd_peer_host: "localhost"
pd_peer_init_port: 7300
pd_peer_alloc_port: 7400
pd_buffer_size: 3758096384 # aligned to 14680064 (256 chunks, ~4 concurrent max-size requests at max_model_len=16384)
pd_buffer_device: "cuda"
nixl_backends: [UCX]